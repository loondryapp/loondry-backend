# Backend environment

# PostgreSQL connection string used by Prisma.
# Copy this file to .env and set the real value locally.
# Supabase direct Postgres requires SSL, so keep ?sslmode=require at the end.
DATABASE_URL="postgresql://USER:PASSWORD@HOST:5432/postgres?sslmode=require"
